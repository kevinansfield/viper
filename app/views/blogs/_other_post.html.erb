<div class="other-post">
	<p>
		<%= link_to post.title, blog_post_path(post.blog, post) %>
		<span class="date"><%= post.created_at %></span>
		<span class="comments"><%= pluralize post.comments.approved.length, 'comment' %></span>
	</p>
</div>