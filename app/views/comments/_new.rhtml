<% remote_form_for(:comment, :url => blog_post_comments_path(:post_id => @post.id), :html => { :class => 'form' }) do |form| %>

<fieldset>
  <legend>New Comment</legend>
  <ol>
  	<li>
		<%= textile_editor :comment, :body, :rows => 5 %>
		<%=textile_helper_link%>
  	</li>
	<li>
		<%= submit_tag "Create" %>
	</li>
  </ol>
</fieldset>
<%= textile_editor_initialize -%>

<% end %>