<% users = User.find_latest -%>
<% unless users.length < 1 -%>
	<%= content_tag :h3, link_to_unless_current('New Users', :controller => 'community') %>
	<ul>
	<% users.each do |user| %>
		<li><%= link_to user.full_name, user_path(user) %></li>
	<% end %>
	</ul>
<% end %>